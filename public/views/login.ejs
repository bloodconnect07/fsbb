<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Log In</title>
        <!-- External CSS -->
        <link rel="stylesheet" href="../css/style.css" />
        <!-- Google Fonts -->
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Roboto&display=swap"
            rel="stylesheet"
        />
    </head>
    <body
        class="w-full h-screen flex flex-col justify-center items-center font-[roboto]"
    >
        <!-- Login -->
        <main
            class="w-[90%] h-screen flex flex-col justify-center items-center gap-10"
        >
            <h1 class="text-5xl font-bold">Log In</h1>
            <form
                action="/login"
                method="POST"
                class="flex flex-col justify-center border-2 border-black gap-7 p-5 w-[30%]"
            >
                <div class="flex flex-col justify-center gap-3">
                    <label class="text-xl font-semibold" for="email"
                        >Email<span>*</span></label
                    >
                    <input
                        class="p-2 border border-black"
                        type="email"
                        id="email"
                        name="email"
                        placeholder="Enter your email"
                        required
                    />
                </div>
                <div class="flex flex-col justify-center gap-3">
                    <label class="text-xl font-semibold" for="password"
                        >Password<span>*</span></label
                    >
                    <input
                        class="p-2 border border-black"
                        type="password"
                        id="password"
                        name="password"
                        placeholder="Enter your password"
                        required
                    />
                </div>
                <div class="flex flex-col justify-center gap-3">
                    <button
                        class="p-2 text-lg font-bold text-white bg-black border-2 border-black hover:text-black hover:bg-white"
                        type="submit"
                    >
                        Log In
                    </button>
                    <button
                        class="flex items-center justify-center gap-2 p-2 text-lg font-bold border-2 border-black hover:text-white hover:bg-black"
                    >
                        <a href="#"
                            ><i class="fa-brands fa-google"></i>Sign Up with
                            Google</a
                        >
                    </button>
                </div>
            </form>
            <p>
                Don&apos;t have an account?
                <a class="underline" href="/signup">Sign Up</a>
            </p>
        </main>
        <!-- Font Awesome -->
        <script
            src="https://kit.fontawesome.com/c46ec1b7c4.js"
            crossorigin="anonymous"
        ></script>
    </body>
</html>
